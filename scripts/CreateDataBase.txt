create table users(
id SERIAL PRIMARY KEY,
full_name VARCHAR(150) UNIQUE NOT NULL,
role INTEGER DEFAULT 0
);

create table customer(
id SERIAL PRIMARY KEY,
full_name varchar(150),
pasport varchar(8)
);

create table orders
(
id SERIAL PRIMARY KEY,
order_date DATE,
deadline DATE,
customer integer references customer ON DELETE SET NULL
);